#include <stdio.h>  
int main(){
    int vida;
    float escudo = 25;
   // bool tienePocion = true;

    //if(vida == 100 && escudo > 0)
      //  printf("El personaje tiene vida y escudo\n");
    //else if(vida > 0 || tienePocion)
      //  printf("Jugador herido pero con posibilidad de recuperarse\n");
    //else if(vida < 0 )
      //  printf("jugador muerto\n");
    //else
      //  printf("Estado desconocido\n");
}

//El programa tiene los siguientes errores:
// 1. La variable `vida` no está inicializada.
// 2. La variable `tienePocion` no está declarada como `bool`,ya que no incluyo la libreria pertinente para usar los booleanos